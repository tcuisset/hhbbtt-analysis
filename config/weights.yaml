puweight:
    name: puWeightRDF
    path: Corrections.LUM.puWeight
    parameters:
        isMC: self.dataset.process.isMC
        year: self.config.year
        isUL: self.dataset.has_tag('ul')

# UL only
# puweightCor:
    # name: puWeightRDF
    # path: Corrections.LUM.puWeightCor
    # parameters:
        # isMC: self.dataset.process.isMC
        # year: self.config.year
        # isUL: self.dataset.has_tag('ul')

dystitching:
    name: DYstitchingRDF
    path: Tools.Tools.dyreweighting
    parameters:
        year: self.config.year
        isDY: self.dataset.process.get_aux('isDY', False)
        isUL: self.dataset.has_tag('ul')

zz_sl_sig_bkg:
    name: ZZBBTauTauFilterRDF
    path: Tools.Tools.ZZAnalysis
    parameters:
        isZZsig: self.dataset.process.get_aux('isZZsignal', False)
        isZZbkg: self.dataset.process.get_aux('isZZbackground', False)