puweight:
    name: puWeightRDF
    path: Corrections.LUM.puWeight
    parameters:
        isMC: self.dataset.process.isMC
        year: self.config.year
        isUL: self.dataset.has_tag('ul')

# UL only
# puweightCor:
    # name: puWeightRDF
    # path: Corrections.LUM.puWeightCor
    # parameters:
        # isMC: self.dataset.process.isMC
        # year: self.config.year
        # isUL: self.dataset.has_tag('ul')

dystitching:
    name: DYstitchingRDF
    path: Tools.Tools.dyreweighting
    parameters:
        year: self.config.year
        isDY: self.dataset.process.get_aux('isDY', False)
        isUL: self.dataset.has_tag('ul')

# very important otherwise counts are wrong for signal
zz_sl_sig_bkg:
    name: ZHBBTauTauFilterRDF
    path: Tools.Tools.ZHAnalysis
    parameters:
        isZHsig: self.dataset.process.get_aux('isZHsignal', [])
        isZHbkg: self.dataset.process.get_aux('isZHbackground', False)